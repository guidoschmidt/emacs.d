# -*- mode: snippet -*-
# name: circle
# key: circ
# --
float circle(in vec2 uv) {
  float circle;
  // a. The DISTANCE from the pixel to the center
  circle = distance(st,vec2(0.5));

  // b. The LENGTH of the vector from the pixel to the center
  // vec2 toCenter = vec2(0.5)-st;
  // circle = length(toCenter);

  // c. The SQUARE ROOT of the vector from the pixel to the center
  // vec2 tC = vec2(0.5)-st;
  // circle = sqrt(tC.x*tC.x+tC.y*tC.y);
  return circle
}
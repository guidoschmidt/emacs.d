;;; clojure.el --- Setup clojure language
;;; Commentary:

;;; Code:
(use-package clojure-mode
  :ensure t
  :defer t)

(use-package cider
  :ensure t
  :defer t)

(provide 'clojure.el)
;;; clojure.el ends here
